# Enumerating elements of a list
fruits = ['apple', 'banana', 'orange']
for index, fruit in enumerate(fruits):
    print(index, fruit)

'''
This code use the enumerate() function. 
It adds a counter to an iterable object (such as a list) and returns it in the form of an enumerate object. 
In this case, it's used to loop through the list fruits and print both the index and the corresponding fruit.
'''
